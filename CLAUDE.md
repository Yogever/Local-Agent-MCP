# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a **Local Agent MCP** project — a Model Context Protocol (MCP) server that exposes local agent capabilities to Claude Code or other MCP-compatible clients.

> Note: This file was generated when the project directory was empty. Update it as the project takes shape.

## Development Setup

Add commands here once the project is initialized (e.g., `npm install`, `pip install -r requirements.txt`).

## Architecture

Document the high-level architecture here as it develops — e.g., which MCP tools are exposed, how the local agent is structured, and how the server communicates with clients.
